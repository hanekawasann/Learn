package com.yukms.learn.java.util;

import org.junit.Assert;
import org.junit.Test;

/**
 * @author hudingpeng hudingpeng@souche.com 2019/11/25 15:49
 */
public class AbstractCollectionTest {
    @Test
    public void test_01() {
        Assert.assertEquals(1 / 2, 1 >> 1);
        Assert.assertEquals(2 / 2, 2 >> 1);
        Assert.assertEquals(3 / 2, 3 >> 1);
        Assert.assertEquals(4 / 2, 4 >> 1);
        Assert.assertEquals(5 / 2, 5 >> 1);
        Assert.assertEquals(6 / 2, 6 >> 1);
    }
}
